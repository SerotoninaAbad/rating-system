import { Map, LatLngFn } from './Map';

interface Position {
  lat: number;
  lng: number;
}

export class Marker {
  constructor(position: Position, map: Map) {
    return;
  }

  setPosition(latLng: LatLngFn) {
    return;
  }
}
